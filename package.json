{
  "name": "personal-website",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "tailwind": "bun run tailwindcss -i ./input.css -o ./public/output.css --watch",
    "start": "bun run index.ts"
  },
  "dependencies": {
    "express": "~4.16.1"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
