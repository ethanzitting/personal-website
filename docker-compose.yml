version: '3.9'

services:
  site:
    tty: true
    restart: unless-stopped
    build:
      context: .
    ports:
      - '127.0.0.1:3000:3000'
    networks:
      - caddy

networks:
  caddy:
    external: true